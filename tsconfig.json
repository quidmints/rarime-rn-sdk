// @generated by expo-module-scripts, with the module settings pinned - see below.
//
// package.json declares no "type", so Node treats build/*.js as CommonJS - but the inherited
// default emitted ESM (`export * from "./Rarime"`), leaving the package neither require()-able nor
// import()-able without extensions. Metro tolerates both, so this only surfaced when a plain-Node
// consumer (quidmints/ibiza's fixture generator) tried to load it.
//
// verbatimModuleSyntax is switched OFF because it is mutually exclusive with emitting CommonJS
// from ESM-syntax sources (TS1295). The type-only import annotations it forced are kept - they
// are correct and harmless - but it can no longer reject the emit.
{
  "extends": "expo-module-scripts/tsconfig.base",
  "compilerOptions": {
    "outDir": "./build",
    "module": "commonjs",
    "moduleResolution": "node",
    "verbatimModuleSyntax": false
  },
  "include": [
    "./src"
  ],
  "exclude": [
    "**/__mocks__/*",
    "**/__tests__/*",
    "**/__rsc_tests__/*"
  ]
}
